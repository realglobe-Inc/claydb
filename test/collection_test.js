/**
 * Test case for collection.
 * Runs with mocha.
 */
'use strict'

const collection = require('../lib/collection.js')
const assert = require('assert')
const co = require('co')

describe('collection', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Collection', () => co(function * () {

  }))
})

/* global describe, before, after, it */
